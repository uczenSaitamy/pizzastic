{% extends 'account/base.html.twig' %}

{% block subtitle %}Account
{% endblock %}

{% block subbody %}
    <div class="flex flex justify-between text-center">
        <p>Yours Addresses</p>
        <a class="btn-g uppercase" href="#">add</a>
    </div>
    <hr class="border-b-2 border-gray-200 mt-5">
    <ul class="list-none flex flex-wrap text-gray-600 p-5">
        {% for address in addresses %}
            <li class="w-1/2 justify-center text-center mb-3">
                <p class="text-xl">{{ address.name }}</p>
                <p class="text-base">{{ address }}</p>

                <a class="btn-o uppercase" href="#">edit</a>
                <a class="btn-r uppercase" href="#">delete</a>
                {# {% include "account/address/form.html.twig" with {'form': addressForm, 'exist': true, 'id': id} only %} #}
            </li>
        {% endfor %}
        {# <li #}
    {# class="flex-1 justify-center text-center"> #}

        {# {% include "account/address/form.html.twig" with {'form': newAddressForm, 'exist': false } only %} #}
        {# </li> #}
    </li>
</ul>{% endblock %}
